/* jshint expr:true */
import {
  fullDate
} from 'hd-ember/helpers/full-date';

describe('FullDateHelper', function() {
  // Replace this with your real tests.
  it('works', function() {
    var result = fullDate(42);
    expect(result).to.be.ok;
  });
});
