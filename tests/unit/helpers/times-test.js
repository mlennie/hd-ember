/* jshint expr:true */
import {
  times
} from 'hd-ember/helpers/times';

describe('TimesHelper', function() {
  // Replace this with your real tests.
  it('works', function() {
    var result = times(42);
    expect(result).to.be.ok;
  });
});
