/* jshint expr:true */
import {
  moment
} from 'hd-ember/helpers/moment';

describe('MomentHelper', function() {
  // Replace this with your real tests.
  it('works', function() {
    var result = moment(42);
    expect(result).to.be.ok;
  });
});
